{
  "name": "aptrust-poc",
  "private": true,
  "version": "2.0.0",
  "description": "AP Trust Local Verification POC \u2014 trust-boundary verification (Chrome MV3 extension + local TypeScript server + admin UI).",
  "repository": {
    "type": "git",
    "url": "https://github.com/Anush-Prabhu/APT1.1.git"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "workspaces": ["server"],
  "scripts": {
    "start": "npm --prefix server run start",
    "dev": "npm --prefix server run dev",
    "build": "npm --prefix server run build",
    "test": "npm --prefix server test"
  }
}
